#include<stdio.h>

int main() {
  /*
    WAP to check whether a given number is positive, negative, or zero.
   */

   /* float anyNumber;
   printf("Please enter any number: ");
   scanf("%f", &anyNumber);

   if(anyNumber > 0) {
    printf("The number is positive: %f", anyNumber);
   } else if (anyNumber < 0) {
    printf("The number is negative: %f", anyNumber);
   } else {
    printf("The number is zero.");
   } */

   /*
     WAP to check whether a given character is an alphabet(uppercase), an alphabet(lowercase), a digit or a special character
    */



  printf("\n");
  return 0;
}
