#include<stdio.h>

int main() {
  /*
    WAP to check whether a given number is positive, negative, or zero.
   */

   /* float anyNumber;
   printf("Please enter any number: ");
   scanf("%f", &anyNumber);

   if(anyNumber > 0) {
    printf("The number is positive: %f", anyNumber);
   } else if (anyNumber < 0) {
    printf("The number is negative: %f", anyNumber);
   } else {
    printf("The number is zero.");
   } */

   /*
     WAP to check whether a given character is an alphabet(uppercase), an alphabet(lowercase), a digit or a special character
    */

  /*char anyCharacter;
  printf("Please enter any character: ");
  scanf("%c", &anyCharacter);

  if( anyCharacter >= 'A' && anyCharacter <= 'Z') {
    printf("It is a Uppercase alphabet: %c", anyCharacter);
  } else if(anyCharacter >= 'a' && anyCharacter <= 'z') {
    printf("It is a lowercase alphabet: %c", anyCharacter);
  } else if(anyCharacter >= '0' && anyCharacter <= '9') {
    printf("It is a digit: %c", anyCharacter);
  } else {
    printf("It is an special character. %c", anyCharacter);
  } */

  /*
    WAP which takes the length of the sides of a triangle as an input. Display whether the triangle is valid or not.
   */

  /*float a, b, c;
  printf("Plese enter the 1st side of triangle: ");
  scanf("%f", &a);
  printf("Plese enter the 2nd side of triangle: ");
  scanf("%f", &b);
  printf("Plese enter the 3rd side of triangle: ");
  scanf("%f", &c);

  if(a > 0 && b > 0 && c > 0 && a + b > c && a + c > b && b + c > a) {

      printf("It is a valid triangle");

  } else {
      printf("It is an invalid triangle");
  } */

  /*
    WAP which takes the month number as an input and display number of days in that month.
   */

   /*int numberOfMonth;
   printf("Please enter number of month: ");
   scanf("%d", &numberOfMonth);

   if(numberOfMonth == 2) {
    printf("It's february.");
   } else if (numberOfMonth == 1 || numberOfMonth == 3 || numberOfMonth == 5 || numberOfMonth == 7 || numberOfMonth == 8 || numberOfMonth == 10 || numberOfMonth == 12) {
    printf("31 days are in the month.");
   } else if(numberOfMonth > 12 || numberOfMonth < 1) {
    printf("It's an invalid month");
   } else {
    printf("30 days are in the month.");
   } */


  printf("\n");
  return 0;
}
