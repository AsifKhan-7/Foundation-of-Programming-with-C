#include<stdio.h>

int main() {
/*
  WAP to check whether a number is positive or non-positive.
 */

 /* int number;
  printf("Please enter any number: ");
  scanf("%d", &number);

  if(number > 0) {
    printf("Number: %d is positive.", number);
  }

  if(number <= 0){
    printf("Number: %d is non-positive.", number);
  } */

 /*
   WAP to chech whether a given number is divisible by 5 or not.
  */

   /* int number, lastDigit;
   printf("Please enter any number: ");
   scanf("%d", &number);

   lastDigit = number % 10;
   if(lastDigit == 5 || lastDigit == 0) {
    printf("Given number: %d is divisible by 5.", number);
   }

   if(lastDigit != 5 && lastDigit != 0) {
   printf("Given number: %d is not divisible by 5.", number);
   } */

 /*
  WAP to check whether a given number is even or an odd number.
 */

 /* int number;
  printf("Please enter any number: ");
  scanf("%d", &number);

  if(number % 2 == 0) {
    printf("Number: %d is even.", number);
  }

  if(number % 2 != 0) {
    printf("Number: %d is odd.", number);

  } */

  /*
  WAP to check whether a given number is even or an odd number without using % operator.
 */

  /*  int number, result;
    printf("Please enter any number: ");
    scanf("%d", &number);
    result = number / 2;
    result = result * 2;

    if(result == number) {
      printf("Number: %d is even." , number);
    }
    if(result != number) {
      printf("Number: %d is odd." , number);
    }*/

    /*
    WAP to check whether a given number is even or an odd number using bitwise operator.
     */

     /*int num, result;
     printf("Please enter any number: ");
     scanf("%d", &num);

     result = num & 1;

     if (result == 1)
     {
      printf("Number: %d is odd.", num);
     }

     if (result == 0)
     {
      printf("Number: %d is even.", num);
     }


      printf("\n");
      return 0; */
}

