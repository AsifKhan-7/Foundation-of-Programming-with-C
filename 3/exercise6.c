#include<stdio.h>

int main() {

//Assume price of 1 USD is INR 84.23. WAP to take the amount in INR and convert it into USD.

/* float USD = 84.23, rupee;
    printf("Please enter amount in INR: ");
    scanf("%f", &rupee);
    USD = rupee / USD;
    printf("Now %f rupee in USD is: %f", rupee, USD); */

/* What will be the value stored in the variable x after executing the following statement?
                  x = 10 > 8 > 4;
*/

    /* int x;
    x = 10 > 8 > 4;
    printf("Now x will: %d", x); */


/* What will be the value stored in the variable x after executing the following statement?
               x = !2 > -2;
*/

  /* int x;
    x = !2 > -2;
    printf("Now x will: %d", x); */

/* What will be the value stored in the variable x after executing the following statement?
               x = 3 < 0 && 5 > 0;
*/
   /* int x;
    x = 3 < 0 && 5 > 0;
    printf("Now x will: %d", x); */

    /*
      WAP to take a three digit number from the user and rotate its digits by one position towards the right.
     */

  /* int number, lastDigit, newNumber;
    printf("Please enter any three digit number: ");
    scanf("%d", &number);

    lastDigit = (number % 10) * 100;
    number = number / 10;
    newNumber = lastDigit + number;
    printf("Number is: %d", newNumber);

    printf("\n");
    return 0; */
}
